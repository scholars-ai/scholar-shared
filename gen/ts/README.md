# @scholars-ai/contracts

Generated TypeScript contracts for Scholars AI. The JSON Schema and OpenAPI sources live in [`scholars-ai/scholar-shared`](https://github.com/scholars-ai/scholar-shared).

```bash
pnpm add -D @scholars-ai/contracts
```

```ts
import type { components, paths } from "@scholars-ai/contracts/core-api";
import type { PlatformProfile } from "@scholars-ai/contracts/contracts";
```

The package is generated and published by GitHub Actions through npm Trusted Publishing. Generated declaration files should not be edited manually.
